# Dependencias de Node (carpeta de módulos instalados)
node_modules/

# Builds/Artefactos de compilación de TypeScript (código JavaScript generado)
dist/
build/
out/
*.tsbuildinfo

# Archivos de configuración de entorno/local (no se deben commitear credenciales o configs locales)
.env
.env.*          # Ignora cualquier variación de archivos de entorno (.env.development, .env.local, etc.)
!.env.example   # (Opcional) No ignores un ejemplo de .env si existe, para documentación de variables
.vscode/
.idea/

# Archivos de registro (logs) y caché
logs/
log/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.cache/
.eslintcache

# Reportes de pruebas y cobertura de código
coverage/
.nyc_output/

# Archivos de sistema/temporales que no aportan al código
.DS_Store
Thumbs.db

# Archivos de datos de MongoDB (BD local, dumps) - opcional, si se generan en el proyecto
data/
dump/